$(document).ready( () => {
    $(".panel-title").on("click", function (event) {
        $("#talentSubOne").attr("class", "collapse");
        // Will need to add others that have 2nd layers
    });
});